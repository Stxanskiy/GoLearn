{
  "n": 19,
  "id": "ch_k8si_6_config",
  "title": "ConfigMap и Secret: настройки отдельно от контейнера",
  "type": "chapter",
  "isFree": false,
  "kind": "lesson",
  "content_html": "<h1>ConfigMap и Secret: настройки отдельно от контейнера</h1>\n\n\n<p>В образ контейнера следует упаковывать само приложение и его зависимости, но не «вшивать» туда параметры окружения. Если совсем коротко: image отвечает на вопрос «что за программа запускается?», тогда как конфигурация отвечает на вопрос «как именно эта программа должна вести себя здесь и сейчас?».</p>\n<p>Возьмём, к примеру, один и тот же backend, работающий в dev, staging и prod. Код везде идентичен, а вот настройки различаются:</p>\n<ul>\n<li>в dev он ходит в тестовую базу;</li>\n<li>в prod он ходит в production-базу;</li>\n<li>в одном окружении новая функция включена, в другом отключена;</li>\n<li>адрес соседнего API в staging отличается от адреса в prod;</li>\n<li>логи в dev пишутся подробно, а в prod более сдержанно.</li>\n</ul>\n<p>Если ради каждого нового адреса базы, включения debug-режима или правки feature flag заново пересобирать image, поставка очень скоро становится хрупкой. Один и тот же код начинает жить в нескольких почти идентичных image: <code>myapp-dev</code>, <code>myapp-stage</code>, <code>myapp-prod</code>. А затем трудно разобраться, где кроется баг: в коде, в image или в настройках.</p>\n<p>Kubernetes предлагает иной путь: image остаётся неизменным, а окружение подаёт настройки рядом с Pod'ом. Благодаря этому приложение становится переносимым: один artifact можно запускать в разных namespace, подменяя только конфигурацию.</p>\n<h2>Что вообще считается настройкой</h2>\n<p>Настройка - это значение, меняющее поведение приложения без правки его кода. Приложение уже умеет это значение читать, а мы лишь сообщаем ему конкретное значение для текущего окружения.</p>\n<p>Простые примеры:</p>\n<table>\n<thead>\n<tr>\n<th>Настройка</th>\n<th>Что означает</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>APP_ENV=prod</code></td>\n<td>в каком режиме работает приложение</td>\n</tr>\n<tr>\n<td><code>LOG_LEVEL=debug</code></td>\n<td>насколько подробно писать логи</td>\n</tr>\n<tr>\n<td><code>DB_HOST=postgres</code></td>\n<td>где находится база данных</td>\n</tr>\n<tr>\n<td><code>DB_PORT=5432</code></td>\n<td>на каком порту слушает база</td>\n</tr>\n<tr>\n<td><code>API_URL=http://orders-api:8080</code></td>\n<td>к какому внутреннему сервису ходить</td>\n</tr>\n<tr>\n<td><code>PAYMENT_API_URL=https://payments.example.com</code></td>\n<td>к какому внешнему сайту или API обращаться</td>\n</tr>\n<tr>\n<td><code>FEATURE_NEW_CHECKOUT=true</code></td>\n<td>включить или выключить конкретную функцию</td>\n</tr>\n<tr>\n<td><code>CACHE_TTL_SECONDS=60</code></td>\n<td>сколько секунд хранить данные в кэше</td>\n</tr>\n</tbody>\n</table>\n<p>Feature flag - это, по сути, тумблер функции. Скажем, разработчики добавили новую страницу оплаты, но хотят сначала включить её только в staging. Тогда код в image уже присутствует, однако настройка <code>FEATURE_NEW_CHECKOUT=false</code> скрывает новую функцию от пользователей в prod.</p>\n<p>Конфиг - понятие более широкое. Это может быть как одна переменная, так и целый файл с настройками. Например, <code>nginx.conf</code>, YAML-файл приложения, набор правил маршрутизации, сертификат, CA bundle или JSON с параметрами подключения.</p>\n<p>Когда настройка хранит пароль, токен или приватный ключ, это уже не рядовая настройка для ConfigMap, а чувствительное значение для Secret. Так, <code>DB_HOST=postgres</code> вполне можно держать в ConfigMap, а <code>DB_PASSWORD=...</code> - в Secret.</p>\n<p>В Kubernetes для этого предусмотрено два базовых объекта:</p>\n<table>\n<thead>\n<tr>\n<th>Объект</th>\n<th>Что хранить</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>ConfigMap</td>\n<td>обычные настройки: порт, режим, имя сервиса</td>\n</tr>\n<tr>\n<td>Secret</td>\n<td>чувствительные значения: пароль, token, ключ</td>\n</tr>\n</tbody>\n</table>\n<p>ConfigMap и Secret допускают подключение либо в виде переменных окружения, либо в виде файлов внутри Pod. Это не две произвольные технические формы, а два привычных способа, которыми обычные Linux-приложения получают свои настройки.</p>\n<h2>Почему это важно</h2>\n<p>Один и тот же image допустимо гонять в dev, staging и prod, подменяя только конфигурацию. За счёт этого поставка становится предсказуемой: artifact один, а настройки разные.</p>\n<pre><code class=\"language-text\">image: my-app:1.0\ndev ConfigMap:     APP_ENV=dev\nprod ConfigMap:    APP_ENV=prod\nprod Secret:       DB_PASSWORD=...\n</code></pre>\n<p>Это напоминает запуск обычной программы в Linux:</p>\n<pre><code class=\"language-bash\">APP_ENV=dev LOG_LEVEL=debug ./app\n</code></pre>\n<p>Разница лишь в том, что в Kubernetes эти значения оформлены как объекты API. Их можно просмотреть, применить через YAML, ограничить через RBAC и одинаковым образом подключить к Pod'ам.</p>\n<h2>Env или файл: зачем два способа</h2>\n<p>Приложение внутри контейнера - это самый обычный процесс Linux. Как правило, оно получает настройки одним из двух способов.</p>\n<p><strong>Переменные окружения</strong> хороши для коротких значений:</p>\n<pre><code class=\"language-text\">APP_ENV=prod\nLOG_LEVEL=info\nAPI_URL=http://orders-api:8080\nFEATURE_NEW_CHECKOUT=true\n</code></pre>\n<p>Именно так чаще всего конфигурируют backend-приложения, worker'ы и CLI-сервисы. Многие фреймворки из коробки читают env: приложение стартует, заглядывает в переменные окружения и понимает, куда подключаться и какие функции задействовать.</p>\n<p><strong>Файлы</strong> хороши тогда, когда приложению требуется именно файл на диске:</p>\n<pre><code class=\"language-text\">/etc/nginx/nginx.conf\n/etc/app/config.yaml\n/etc/tls/tls.crt\n/etc/tls/tls.key\n/etc/ssl/certs/ca.pem\n</code></pre>\n<p>Таким путём обычно подают конфиги nginx, prometheus, redis, TLS-сертификаты, CA-сертификаты, ключи, объёмные YAML/JSON-конфиги или настройки, которые приложение перечитывает по пути к файлу.</p>\n<p>Пример разницы:</p>\n<ul>\n<li>backend берёт <code>DB_HOST</code> и <code>DB_PORT</code> из env - это короткие строки, их удобно передавать переменными;</li>\n<li>nginx читает <code>/etc/nginx/conf.d/default.conf</code> - ему требуется файл конфигурации;</li>\n<li>приложение подключается к внешнему HTTPS API с корпоративным CA - ему может пригодиться файл <code>ca.pem</code>;</li>\n<li>приложение применяет TLS-сертификат - как правило, это два файла <code>tls.crt</code> и <code>tls.key</code>, а не одна короткая переменная.</li>\n</ul>\n<blockquote>\n<p><strong>Важная идея:</strong> Kubernetes не вынуждает приложение знать о существовании Kubernetes. Мы всего лишь передаём настройки в том формате, который приложению уже понятен: env-переменной или файлом.</p>\n</blockquote>\n<h2>ConfigMap: обычная конфигурация</h2>\n<p>ConfigMap годится для данных, которые не относятся к секретам:</p>\n<ul>\n<li>режим работы: <code>APP_ENV=dev</code>;</li>\n<li>уровень логирования: <code>LOG_LEVEL=info</code>;</li>\n<li>адрес базы или другого сервиса без пароля: <code>DB_HOST=postgres</code>, <code>API_URL=http://api</code>;</li>\n<li>адрес внешнего API или сайта: <code>PAYMENT_API_URL=https://payments.example.com</code>;</li>\n<li>переключатели функций: <code>FEATURE_NEW_UI=true</code>, <code>FEATURE_NEW_CHECKOUT=false</code>;</li>\n<li>текстовый конфиг nginx, redis или приложения;</li>\n<li>параметры вроде timeout, cache TTL, лимитов и режимов работы.</li>\n</ul>\n<p>ConfigMap не стоит применять для паролей и токенов. Да, технически туда ложится любая строка, но назначение этого объекта именно в том, чтобы отделять обычную конфигурацию от чувствительных данных.</p>\n<p>Модель простая:</p>\n<pre><code class=\"language-text\">ConfigMap -&gt; Pod env/file -&gt; приложение читает настройку\n</code></pre>\n<p>Приложение, как правило, не подозревает, что значение пришло из Kubernetes. Для него это рядовая переменная окружения или рядовой файл.</p>\n<h2>Secret не значит “магически безопасно”</h2>\n<p>Secret в Kubernetes отделяет чувствительные данные от обычных манифестов, однако не делает их невидимыми для всех подряд. Доступ по-прежнему регулируется RBAC, namespace'ами и политиками. Поэтому не стоит выводить секреты в логи и держать их в публичном Git.</p>\n<p>Secret нужен для значений, которые нельзя ненароком показать всем:</p>\n<ul>\n<li>пароль базы;</li>\n<li>API token;</li>\n<li>приватный ключ;</li>\n<li>credentials к внешнему сервису.</li>\n</ul>\n<p>Преимущество Secret перед ConfigMap в том, что Kubernetes и окружающие его инструменты трактуют это как чувствительные данные. Но Secret всё равно не отменяет базовых правил безопасности. Если у пользователя есть возможность читать Secret через API, он получит и само значение.</p>\n<h2>Создание из literal и из файла</h2>\n<p>ConfigMap из пар:</p>\n<pre><code class=\"language-bash\">kubectl create configmap app-config \\\n  --from-literal=APP_ENV=dev \\\n  --from-literal=LOG_LEVEL=debug\n</code></pre>\n<p>ConfigMap из файла:</p>\n<pre><code class=\"language-bash\">kubectl create configmap nginx-conf --from-file=nginx.conf\n</code></pre>\n<p>Secret:</p>\n<pre><code class=\"language-bash\">kubectl create secret generic db-secret \\\n  --from-literal=DB_PASSWORD=secret\n</code></pre>\n<p>Проверить:</p>\n<pre><code class=\"language-bash\">kubectl get configmap app-config -o yaml\nkubectl get secret db-secret -o yaml\n</code></pre>\n<h2>Подключение как env</h2>\n<pre><code class=\"language-yaml\">envFrom:\n- configMapRef:\n    name: app-config\n- secretRef:\n    name: db-secret\n</code></pre>\n<p>Либо отдельная переменная:</p>\n<pre><code class=\"language-yaml\">env:\n- name: APP_ENV\n  valueFrom:\n    configMapKeyRef:\n      name: app-config\n      key: APP_ENV\n</code></pre>\n<p>Когда Pod поднялся, можно проверить:</p>\n<pre><code class=\"language-bash\">kubectl exec app-pod -- env | grep APP_ENV\n</code></pre>\n<p><code>envFrom</code> подтягивает все ключи из ConfigMap или Secret как переменные окружения. Это быстро и удобно в учебных задачах, но в production порой предпочитают явно перечислять нужные ключи через <code>valueFrom</code>, чтобы случайно не протащить лишнее.</p>\n<h2>Подключение как файл</h2>\n<p>Часть приложений ждёт не переменные окружения, а именно конфигурационный файл. В таком случае ConfigMap или Secret можно смонтировать как volume:</p>\n<pre><code class=\"language-yaml\">volumes:\n- name: app-config\n  configMap:\n    name: app-config\ncontainers:\n- name: app\n  image: nginx:alpine\n  volumeMounts:\n  - name: app-config\n    mountPath: /etc/app-config\n</code></pre>\n<p>Если в ConfigMap присутствует ключ <code>app.conf</code>, внутри контейнера возникнет файл:</p>\n<pre><code class=\"language-text\">/etc/app-config/app.conf\n</code></pre>\n<p>Так обычно подают конфиги nginx, prometheus, redis и приложений, читающих YAML/INI/JSON-файлы.</p>\n<p>Важно различать два блока: <code>volumes</code> и <code>volumeMounts</code>.</p>\n<p><code>volumes</code> располагается в <code>spec.template.spec</code> Pod'а. Здесь мы сообщаем Kubernetes: \"возьми данные из этого источника и подготовь их как volume\". Источником способны выступать ConfigMap, Secret, emptyDir, PersistentVolumeClaim и другие типы.</p>\n<p><code>volumeMounts</code> располагается внутри конкретного контейнера. Здесь мы говорим: \"volume с таким именем помести внутрь этого контейнера по такому пути\".</p>\n<p>То есть схема такая:</p>\n<pre><code class=\"language-text\">ConfigMap/Secret -&gt; volumes -&gt; volumeMounts -&gt; файл внутри контейнера\n</code></pre>\n<p>Пример:</p>\n<pre><code class=\"language-yaml\">volumes:\n  - name: config-volume     # внутреннее имя volume внутри Pod\n    configMap:\n      name: app-file-config # имя ConfigMap в Kubernetes\n\ncontainers:\n  - name: app\n    volumeMounts:\n      - name: config-volume # ссылка на volume выше\n        mountPath: /etc/myapp\n</code></pre>\n<p>Если в ConfigMap <code>app-file-config</code> присутствует ключ <code>app.yaml</code>, то внутри контейнера возникнет файл <code>/etc/myapp/app.yaml</code>.</p>\n<p>Один volume разрешается подключить сразу к нескольким контейнерам одного Pod'а. И наоборот: один контейнер может подключить несколько volume в разные директории.</p>\n<h2>Цельный пример Deployment с env и файлами</h2>\n<p>Ниже - учебный пример, в котором в рамках одного Deployment показаны разные способы подключения. В настоящей работе смешивать их все сразу не обязательно: выбирай тот способ, который подходит конкретному приложению.</p>\n<p>Допустим, в кластере уже присутствуют:</p>\n<ul>\n<li>ConfigMap <code>app-settings</code> с короткими настройками: <code>APP_ENV</code>, <code>LOG_LEVEL</code>, <code>FEATURE_NEW_UI</code>;</li>\n<li>Secret <code>db-secret</code> с ключом <code>DB_PASSWORD</code>;</li>\n<li>ConfigMap <code>app-file-config</code> с файлом <code>app.yaml</code>;</li>\n<li>Secret <code>app-tls</code> с файлами <code>tls.crt</code> и <code>tls.key</code>.</li>\n</ul>\n<p>Цельный Deployment:</p>\n<pre><code class=\"language-yaml\">apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: config-demo\nspec:\n  replicas: 2\n  selector:\n    matchLabels:\n      app: config-demo\n  template:\n    metadata:\n      labels:\n        app: config-demo\n    spec:\n      # volumes объявляются на уровне Pod template.\n      # Здесь мы говорим Kubernetes, из каких объектов взять файлы.\n      volumes:\n        - name: app-config-files\n          configMap:\n            name: app-file-config\n        - name: tls-files\n          secret:\n            secretName: app-tls\n\n      containers:\n        - name: app\n          image: nginx:alpine\n\n          # envFrom подключает все ключи ConfigMap как env-переменные.\n          # Например APP_ENV, LOG_LEVEL, FEATURE_NEW_UI.\n          envFrom:\n            - configMapRef:\n                name: app-settings\n\n          # env с valueFrom подключает конкретный ключ.\n          # Так удобнее явно передать один пароль или один важный параметр.\n          env:\n            - name: DB_PASSWORD\n              valueFrom:\n                secretKeyRef:\n                  name: db-secret\n                  key: DB_PASSWORD\n\n          # volumeMounts подключает подготовленные выше volumes\n          # в файловую систему именно этого контейнера.\n          volumeMounts:\n            - name: app-config-files\n              mountPath: /etc/myapp\n              readOnly: true\n            - name: tls-files\n              mountPath: /etc/tls\n              readOnly: true\n</code></pre>\n<p>Что окажется внутри контейнера:</p>\n<pre><code class=\"language-text\">APP_ENV, LOG_LEVEL, FEATURE_NEW_UI     # пришли из ConfigMap app-settings через envFrom\nDB_PASSWORD                            # пришёл из Secret db-secret через secretKeyRef\n/etc/myapp/app.yaml                    # пришёл из ConfigMap app-file-config как файл\n/etc/tls/tls.crt и /etc/tls/tls.key     # пришли из Secret app-tls как файлы\n</code></pre>\n<p>Если приложение берёт настройки из env, оно возьмёт <code>APP_ENV</code> или <code>DB_PASSWORD</code>. Если же приложение ждёт файл, оно прочитает <code>/etc/myapp/app.yaml</code> или TLS-файлы из <code>/etc/tls</code>.</p>\n<h2>Base64 в Secret</h2>\n<p>В YAML Secret значения в <code>data</code> закодированы через base64:</p>\n<pre><code class=\"language-bash\">kubectl get secret db-secret -o jsonpath='{.data.DB_PASSWORD}' | base64 -d\n</code></pre>\n<p>Base64 - это кодирование, а вовсе не шифрование. Любой, у кого есть RBAC-доступ на чтение Secret, сможет декодировать значение. Именно поэтому доступ к Secret нужно ограничивать.</p>\n<h2>Что происходит при изменении</h2>\n<p>Если ты поправил ConfigMap или Secret, уже работающее приложение не всегда заметит изменение мгновенно.</p>\n<p>Для переменных окружения правило нехитрое: env передаётся контейнеру в момент старта. Если ConfigMap изменился, старый процесс сам по себе новых env не получит. Как правило, Pod приходится перезапустить или выполнить rollout.</p>\n<p>Для mounted files Kubernetes может обновить файлы спустя некоторое время, но приложению нужно самому перечитать конфиг. Многие приложения без reload этого не делают.</p>\n<p>Практическая привычка:</p>\n<pre><code class=\"language-bash\">kubectl get configmap app-config -o yaml\nkubectl get secret db-secret -o yaml\nkubectl describe pod app-pod\nkubectl exec app-pod -- env | grep APP_ENV\n</code></pre>\n<p>Сначала проверь сам объект ConfigMap/Secret, затем - как он подключён к Pod, и только после этого смотри поведение приложения.</p>\n<h2>Типичные ошибки</h2>\n<table>\n<thead>\n<tr>\n<th>Симптом</th>\n<th>Возможная причина</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>переменной нет внутри Pod</td>\n<td>ConfigMap не подключён или key называется иначе</td>\n</tr>\n<tr>\n<td>Pod не стартует</td>\n<td>ссылка на несуществующий ConfigMap/Secret</td>\n</tr>\n<tr>\n<td>значение старое</td>\n<td>Pod не перезапущен после изменения env</td>\n</tr>\n<tr>\n<td>Secret виден в логах</td>\n<td>приложение печатает чувствительные переменные</td>\n</tr>\n<tr>\n<td>YAML применился, но приложение не изменилось</td>\n<td>template Pod не обновился или приложение не перечитало файл</td>\n</tr>\n</tbody>\n</table>\n<p>Ключевая мысль: конфигурация должна жить в стороне от image, но это не означает “без контроля”. Для надёжной эксплуатации нужно понимать, где хранится значение, каким путём оно попадает в Pod и в какой момент приложение его читает.</p>\n<p>Перед лабораторной держи в голове такой маршрут: объект ConfigMap/Secret создан, Pod на него ссылается, контейнер получил env или файл, приложение прочитало значение.</p>f:[\"$\",\"div\",null,{\"className\":\"w-full min-w-0 flex-grow\",\"children\":[\"$\",\"div\",null,{\"className\":\"h-full w-full min-w-0 mx-auto\",\"children\":[\"$\",\"div\",null,{\"className\":\"h-full\",\"children\":[[\"$\",\"div\",null,{\"className\":\"max-w-7xl mx-auto w-full px-6\",\"children\":[[\"$\",\"div\",null,{\"className\":\"flex flex-row flex-wrap gap-2 items-center py-4\",\"children\":[[\"$\",\"$L27\",null,{\"courseId\":\"module_k8s_intro\",\"previousChapterId\":\"ch_k8si_7_observe\",\"nextChapterId\":\"ch_k8si_lab4\"}],[\"$\",\"$L28\",null,{\"courseId\":\"module_k8s_intro\",\"chapterId\":\"ch_k8si_6_config\",\"isCompleted\":\"$undefined\",\"nextChapterId\":\"ch_k8si_lab4\"}]]}],[\"$\",\"h2\",null,{\"className\":\"text-2xl font-semibold mb-4 text-center\",\"children\":\"ConfigMap и Secret: настройки отдельно от контейнера\"}]]}],[\"$\",\"div\",null,{\"className\":\"h-full w-full max-w-7xl mx-auto px-3 sm:px-6 lg:px-0\",\"children\":[\"$\",\"$L29\",null,{\"content\":\"$2a\",\"courseId\":\"module_k8s_intro\",\"chapterId\":\"ch_k8si_6_config\",\"isCompleted\":false,\"previousChapterId\":\"ch_k8si_7_observe\",\"nextChapterId\":\"ch_k8si_lab4\"}]}]]}]}]}]",
  "tasks": [],
  "vm_image": null,
  "vm_init": null
}