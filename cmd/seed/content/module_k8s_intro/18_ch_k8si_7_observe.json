{
  "n": 18,
  "id": "ch_k8si_7_observe",
  "title": "Как смотреть уже работающее приложение в Kubernetes",
  "type": "chapter",
  "isFree": false,
  "kind": "lesson",
  "content_html": "<h1>Как смотреть уже работающее приложение в Kubernetes</h1>\n\n\n<p>До этого мы часто создавали объекты командами: <code>kubectl run</code>, <code>kubectl create deployment</code>, <code>kubectl expose</code>. Это удобно для обучения: команда короткая, результат видно сразу.</p>\n<p>В реальной работе обычно наоборот: приложение уже где-то работает, его манифесты лежат в Git, а твоя задача — понять, что сейчас происходит в кластере. Нужно посмотреть объекты, связать их между собой, найти отличие от ожидаемого состояния и собрать понятную картину.</p>\n<p>Эта глава про такой рабочий навык: не “создать с нуля”, а обследовать уже запущенное приложение.</p>\n<blockquote>\n<p><strong>Важная идея:</strong> Kubernetes — это не только запуск объектов, но и постоянное чтение текущего состояния. DevOps, SRE и администратор большую часть времени смотрят, сравнивают, проверяют и только потом меняют.</p>\n</blockquote>\n<h2>Начни с инвентаризации</h2>\n<p>Первый шаг — понять, какие объекты вообще есть в namespace:</p>\n<pre><code class=\"language-bash\">kubectl get all\nkubectl get deployment,replicaset,pod,service\nkubectl get configmap,secret\n</code></pre>\n<p><code>kubectl get all</code> удобен как быстрый обзор, но он не показывает вообще все типы объектов. Например, ConfigMap и Secret лучше запрашивать отдельно.</p>\n<p>Полезно смотреть не только имена:</p>\n<pre><code class=\"language-bash\">kubectl get pods -o wide\nkubectl get svc -o wide\nkubectl get deploy -o wide\n</code></pre>\n<p><code>-o wide</code> добавляет больше колонок: IP Pod'а, Node, selectors, иногда дополнительные детали. Это быстрее, чем сразу открывать полный YAML.</p>\n<h2>Иди сверху вниз</h2>\n<p>Если приложение запущено через Deployment, удобная цепочка такая:</p>\n<pre><code class=\"language-text\">Deployment -&gt; ReplicaSet -&gt; Pod\n</code></pre>\n<p>Deployment описывает желаемое состояние: сколько реплик, какой image, какой Pod template. ReplicaSet следит за количеством Pod'ов. Pod уже запускает контейнер.</p>\n<p>Команды:</p>\n<pre><code class=\"language-bash\">kubectl get deployment web\nkubectl get replicaset\nkubectl get pods -l app=web\n</code></pre>\n<p>Если есть Service, добавляется ещё одна цепочка:</p>\n<pre><code class=\"language-text\">Service -&gt; Endpoints -&gt; Pod IP:port\n</code></pre>\n<p>Команды:</p>\n<pre><code class=\"language-bash\">kubectl get svc web-svc\nkubectl get endpoints web-svc\nkubectl get pods -l app=web -o wide\n</code></pre>\n<p>Так ты проверяешь, что Service действительно нашёл Pod'ы, а не просто существует как пустой объект.</p>\n<h2><code>describe</code>: объяснение объекта</h2>\n<p><code>get</code> отвечает на вопрос “что есть?”. <code>describe</code> отвечает на вопрос “что с этим происходит?”.</p>\n<pre><code class=\"language-bash\">kubectl describe deployment web\nkubectl describe pod web-abc123\nkubectl describe service web-svc\n</code></pre>\n<p>В <code>describe pod</code> особенно важны:</p>\n<ul>\n<li><code>State</code>;</li>\n<li><code>Ready</code>;</li>\n<li><code>Restart Count</code>;</li>\n<li><code>Events</code>;</li>\n<li>ошибки image pull;</li>\n<li>ошибки монтирования ConfigMap/Secret;</li>\n<li>проблемы с resources.</li>\n</ul>\n<p>В <code>describe service</code> важны:</p>\n<ul>\n<li><code>Selector</code>;</li>\n<li><code>IP</code>;</li>\n<li><code>Port</code>;</li>\n<li><code>TargetPort</code>;</li>\n<li><code>Endpoints</code>.</li>\n</ul>\n<p>Если у Service пустые Endpoints, проблема обычно не в DNS. Чаще всего selector Service не совпал с labels Pod'ов или Pod'ы ещё не Ready.</p>\n<h2>YAML текущего объекта</h2>\n<p>Любой объект можно вывести в YAML:</p>\n<pre><code class=\"language-bash\">kubectl get deployment web -o yaml\nkubectl get pod web-abc123 -o yaml\nkubectl get service web-svc -o yaml\n</code></pre>\n<p>Можно сохранить в файл:</p>\n<pre><code class=\"language-bash\">kubectl get deployment web -o yaml &gt; web-deployment-current.yaml\nkubectl get service web-svc -o yaml &gt; web-svc-current.yaml\n</code></pre>\n<p>Это полезно, когда нужно:</p>\n<ul>\n<li>сравнить кластер с манифестом из Git;</li>\n<li>приложить состояние объекта к задаче или инциденту;</li>\n<li>понять, какие defaults добавил Kubernetes;</li>\n<li>посмотреть полный <code>spec</code> и <code>status</code>.</li>\n</ul>\n<p>Но есть важная оговорка.</p>\n<blockquote>\n<p><strong>Важная идея:</strong> YAML, выгруженный из кластера через <code>kubectl get -o yaml</code>, — это снимок текущего объекта, а не готовый чистый manifest для Git. В нём есть служебные поля: <code>status</code>, <code>uid</code>, <code>resourceVersion</code>, <code>managedFields</code>, timestamps. Их обычно не хранят в исходных манифестах.</p>\n</blockquote>\n<p>Чтобы вывод был чуть чище, можно убрать managedFields:</p>\n<pre><code class=\"language-bash\">kubectl get deployment web -o yaml --show-managed-fields=false\n</code></pre>\n<p>Но <code>status</code> и часть metadata всё равно останутся. Для Git обычно оставляют только то, что описывает желаемое состояние: <code>apiVersion</code>, <code>kind</code>, нужные <code>metadata</code>, <code>spec</code>.</p>\n<h2>Как менять уже существующий объект</h2>\n<p>Посмотреть объект - это только половина работы. Иногда после диагностики нужно быстро исправить поле: image, replicas, selector, <code>targetPort</code>, env или resources.</p>\n<p>Есть два распространённых пути.</p>\n<h3>Быстрый путь: kubectl edit</h3>\n<p><code>kubectl edit</code> открывает текущий объект в редакторе:</p>\n<pre><code class=\"language-bash\">kubectl edit deployment web\nkubectl edit service web-svc\nkubectl edit configmap app-config\n</code></pre>\n<p>Ты меняешь YAML, сохраняешь файл, и <code>kubectl</code> отправляет изменение обратно в API Server.</p>\n<p>Это удобно для срочного исправления или учебной практики. Например, быстро поправить <code>replicas</code>, image tag или <code>targetPort</code>.</p>\n<p>Но у <code>kubectl edit</code> есть важный минус: изменение легко потерять. Если реальные манифесты живут в Git, а ты исправил только объект в кластере, следующий деплой или GitOps-синхронизация может вернуть старое состояние.</p>\n<p>Поэтому <code>edit</code> - это быстрый инструмент, а не основной production workflow.</p>\n<h3>Стандартный путь: сохранить YAML, изменить, применить</h3>\n<p>Более воспроизводимый путь:</p>\n<pre><code class=\"language-bash\">kubectl get deployment web -o yaml --show-managed-fields=false &gt; web-deployment.yaml\n</code></pre>\n<p>Дальше открой файл и оставь только нужное желаемое состояние. Обычно удаляют:</p>\n<ul>\n<li><code>status</code>;</li>\n<li><code>metadata.uid</code>;</li>\n<li><code>metadata.resourceVersion</code>;</li>\n<li><code>metadata.creationTimestamp</code>;</li>\n<li><code>metadata.generation</code>;</li>\n<li><code>metadata.managedFields</code>, если они есть.</li>\n</ul>\n<p>После правки применяют файл:</p>\n<pre><code class=\"language-bash\">kubectl apply -f web-deployment.yaml\nkubectl rollout status deployment/web\n</code></pre>\n<p>Для Service аналогично:</p>\n<pre><code class=\"language-bash\">kubectl get service web-svc -o yaml --show-managed-fields=false &gt; web-svc.yaml\nkubectl apply -f web-svc.yaml\n</code></pre>\n<p>Такой подход полезен, когда нужно:</p>\n<ul>\n<li>зафиксировать, что именно менялось;</li>\n<li>повторить изменение в другом namespace;</li>\n<li>положить исправление в Git;</li>\n<li>отправить diff на review;</li>\n<li>избежать ручной правки “только в кластере”.</li>\n</ul>\n<p>Рабочее правило простое: <code>kubectl edit</code> подходит для быстрого ручного исправления, но правильное долгосрочное состояние должно оказаться в manifest-файле и в Git.</p>\n<h2>Логи контейнера</h2>\n<p>Если Pod Running, но приложение не работает, смотри logs:</p>\n<pre><code class=\"language-bash\">kubectl logs pod/web-abc123\nkubectl logs deployment/web\nkubectl logs deployment/web --tail=50\nkubectl logs deployment/web -f\n</code></pre>\n<p><code>--tail=50</code> показывает последние строки. <code>-f</code> подписывается на поток логов.</p>\n<p>Если контейнер падал и перезапускался, полезен предыдущий запуск:</p>\n<pre><code class=\"language-bash\">kubectl logs pod/web-abc123 --previous\n</code></pre>\n<p>Это часто помогает при <code>CrashLoopBackOff</code>: текущий контейнер уже перезапускается, а ошибка была напечатана перед прошлым падением.</p>\n<h2>Exec внутрь Pod</h2>\n<p><code>kubectl exec</code> похож на <code>docker exec</code>: можно выполнить команду внутри контейнера.</p>\n<pre><code class=\"language-bash\">kubectl exec web-abc123 -- printenv\nkubectl exec web-abc123 -- sh\nkubectl exec web-abc123 -- cat /etc/resolv.conf\n</code></pre>\n<p>Если в Pod несколько контейнеров, укажи контейнер:</p>\n<pre><code class=\"language-bash\">kubectl exec web-abc123 -c app -- printenv\n</code></pre>\n<p><code>exec</code> нужен не для постоянной ручной настройки приложения, а для диагностики: посмотреть env, файл конфига, DNS-настройки, доступность соседнего Service.</p>\n<h2>Проверка сетевого пути</h2>\n<p>Для Service полезно проверять по слоям:</p>\n<pre><code class=\"language-text\">DNS имя найдено?\nService существует?\nEndpoints есть?\ntargetPort правильный?\nПриложение отвечает HTTP/TCP?\n</code></pre>\n<p>Команды:</p>\n<pre><code class=\"language-bash\">kubectl get svc web-svc\nkubectl get endpoints web-svc\nkubectl describe svc web-svc\n</code></pre>\n<p>Проверка изнутри кластера:</p>\n<pre><code class=\"language-bash\">kubectl run debug --image=busybox:1.28 --restart=Never --command -- sleep 3600\nkubectl wait --for=condition=Ready pod/debug --timeout=60s\nkubectl exec debug -- nslookup web-svc\nkubectl exec debug -- wget -qO- http://web-svc\nkubectl delete pod debug\n</code></pre>\n<p>Почему из Pod? Потому что <code>ClusterIP</code> Service живёт внутри сети Kubernetes. Проверка снаружи VM и проверка из Pod — это разные сетевые пути.</p>\n<h2>Rollout и история изменений</h2>\n<p>Для Deployment всегда проверяй rollout:</p>\n<pre><code class=\"language-bash\">kubectl rollout status deployment/web\nkubectl rollout history deployment/web\nkubectl describe deployment web\n</code></pre>\n<p>Если обновление зависло:</p>\n<pre><code class=\"language-bash\">kubectl get pods -l app=web\nkubectl describe pod &lt;pod-name&gt;\nkubectl logs &lt;pod-name&gt;\n</code></pre>\n<p>Откат:</p>\n<pre><code class=\"language-bash\">kubectl rollout undo deployment/web\n</code></pre>\n<p>Откат — это не магия. Kubernetes возвращает Pod template к прошлой ревизии Deployment и запускает новый rollout.</p>\n<h2>Events</h2>\n<p>Events показывают, что Kubernetes пытался сделать и почему не получилось:</p>\n<pre><code class=\"language-bash\">kubectl get events --sort-by=.lastTimestamp\n</code></pre>\n<p>Примеры полезных событий:</p>\n<ul>\n<li>image не скачался;</li>\n<li>Pod не запланирован из-за ресурсов;</li>\n<li>Secret или ConfigMap не найден;</li>\n<li>probe падает;</li>\n<li>volume не смонтировался.</li>\n</ul>\n<p>Events особенно полезны, когда объект “просто висит” в <code>Pending</code>, <code>ContainerCreating</code>, <code>ErrImagePull</code> или <code>CrashLoopBackOff</code>.</p>\n<h2>Мини-чеклист разбора приложения</h2>\n<ol>\n<li>Узнай namespace: <code>kubectl config view --minify</code>.</li>\n<li>Посмотри основные объекты: <code>kubectl get deploy,pod,svc</code>.</li>\n<li>Свяжи их через labels/selectors.</li>\n<li>Проверь Service и Endpoints.</li>\n<li>Посмотри <code>describe</code> проблемного объекта.</li>\n<li>Посмотри logs.</li>\n<li>Если нужно, зайди через <code>exec</code>.</li>\n<li>Сохрани YAML текущего состояния, если нужно приложить его к задаче.</li>\n<li>Сравни с манифестом из Git.</li>\n<li>Только после этого меняй manifest или выполняй исправляющую команду.</li>\n</ol>\n<p>Такой подход экономит время. Ты не угадываешь, где проблема, а проходишь приложение по цепочке: объект, controller, Pod, Service, сеть, логи.</p>f:[\"$\",\"div\",null,{\"className\":\"w-full min-w-0 flex-grow\",\"children\":[\"$\",\"div\",null,{\"className\":\"h-full w-full min-w-0 mx-auto\",\"children\":[\"$\",\"div\",null,{\"className\":\"h-full\",\"children\":[[\"$\",\"div\",null,{\"className\":\"max-w-7xl mx-auto w-full px-6\",\"children\":[[\"$\",\"div\",null,{\"className\":\"flex flex-row flex-wrap gap-2 items-center py-4\",\"children\":[[\"$\",\"$L27\",null,{\"courseId\":\"module_k8s_intro\",\"previousChapterId\":\"ch_k8si_lab_headless_stateful\",\"nextChapterId\":\"ch_k8si_6_config\"}],[\"$\",\"$L28\",null,{\"courseId\":\"module_k8s_intro\",\"chapterId\":\"ch_k8si_7_observe\",\"isCompleted\":\"$undefined\",\"nextChapterId\":\"ch_k8si_6_config\"}]]}],[\"$\",\"h2\",null,{\"className\":\"text-2xl font-semibold mb-4 text-center\",\"children\":\"Как смотреть уже работающее приложение в Kubernetes\"}]]}],[\"$\",\"div\",null,{\"className\":\"h-full w-full max-w-7xl mx-auto px-3 sm:px-6 lg:px-0\",\"children\":[\"$\",\"$L29\",null,{\"content\":\"$2a\",\"courseId\":\"module_k8s_intro\",\"chapterId\":\"ch_k8si_7_observe\",\"isCompleted\":false,\"previousChapterId\":\"ch_k8si_lab_headless_stateful\",\"nextChapterId\":\"ch_k8si_6_config\"}]}]]}]}]}]",
  "tasks": [],
  "vm_image": null,
  "vm_init": null
}