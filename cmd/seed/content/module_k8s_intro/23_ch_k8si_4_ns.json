{
  "n": 23,
  "id": "ch_k8si_4_ns",
  "title": "Namespaces и ограничение ресурсов",
  "type": "chapter",
  "isFree": false,
  "kind": "lesson",
  "content_html": "<h1>Namespaces и ограничение ресурсов</h1>\n\n\n<p>В Kubernetes приложение стартует не «где-то абстрактно на сервере». Оно стартует в общем кластере, где по соседству могут жить dev-окружения, staging, системные компоненты и приложения разных команд.</p>\n<p>Из-за этого Kubernetes должен закрыть два практических вопроса:</p>\n<ul>\n<li>где логически расположены объекты приложения;</li>\n<li>сколько CPU и памяти приложению позволено запросить и по факту потреблять.</li>\n</ul>\n<p>Первый вопрос решает <strong>Namespace</strong>. Второй - <code>resources.requests</code>, <code>resources.limits</code>, а на уровне namespace - <code>LimitRange</code> и <code>ResourceQuota</code>.</p>\n<blockquote>\n<p><strong>Важная идея:</strong> сперва ресурсы указываются на уровне контейнера в Pod template, и только затем их могут ограничивать правила namespace.</p>\n</blockquote>\n<p>Иначе говоря, типичный разработческий манифест чаще всего несёт <code>resources</code> внутри контейнера. А <code>ResourceQuota</code> и <code>LimitRange</code> обычно настраивает администратор платформы, чтобы отдельный namespace не забрал под себя весь кластер.</p>\n<h2>Namespace — рабочая область</h2>\n<p>Namespace - это логическая область внутри кластера. В различных namespace вполне могут сосуществовать объекты с одинаковыми именами:</p>\n<pre><code class=\"language-text\">dev/web\nstage/web\nprod/web\n</code></pre>\n<p>С точки зрения Kubernetes это разные Deployment'ы, поскольку namespace входит в адрес объекта.</p>\n<p>Команды:</p>\n<pre><code class=\"language-bash\">kubectl get pods\nkubectl get pods -n dev\nkubectl config view --minify -o jsonpath='{.contexts[0].context.namespace}'\nkubectl config set-context --current --namespace=dev\n</code></pre>\n<p>В учебном кластере namespace обычно уже подготовлен за тебя. Ты работаешь внутри своего namespace, а границы между namespace регулируются RBAC. Если команда отдаёт <code>Forbidden</code>, проверь namespace, context и уровень объекта: прикладной это ресурс или platform/admin ресурс.</p>\n<p>Сам по себе namespace не является отдельной виртуальной машиной. Это не полноценная security-изоляция. Но он удобен в роли границы для:</p>\n<ul>\n<li>имён объектов;</li>\n<li>прав доступа;</li>\n<li>лимитов ресурсов;</li>\n<li>разделения окружений и команд.</li>\n</ul>\n<h2>Зачем вообще ограничивать ресурсы</h2>\n<p>Контейнер способен потреблять CPU и память. Если приложения никак не ограничивать, одно неудачное приложение может отъесть слишком много памяти, помешать другим Pod'ам или вообще загнать node в тяжёлое состояние.</p>\n<p>В Docker ты мог запускать контейнер с ограничениями наподобие <code>--memory</code> и <code>--cpus</code>. В Kubernetes это описывают в манифесте:</p>\n<pre><code class=\"language-yaml\">resources:\n  requests:\n    cpu: \"100m\"\n    memory: \"128Mi\"\n  limits:\n    cpu: \"500m\"\n    memory: \"256Mi\"\n</code></pre>\n<p>Эти поля живут внутри контейнера:</p>\n<pre><code class=\"language-yaml\">apiVersion: v1\nkind: Pod\nmetadata:\n  name: web\nspec:\n  containers:\n    - name: nginx\n      image: nginx:alpine\n      resources:\n        requests:\n          cpu: \"100m\"\n          memory: \"128Mi\"\n        limits:\n          cpu: \"500m\"\n          memory: \"256Mi\"\n</code></pre>\n<h2>Requests и limits простыми словами</h2>\n<p><code>requests</code> - сколько ресурсов Pod просит зарезервировать при размещении.</p>\n<p><code>limits</code> - потолок, выше которого контейнеру подниматься нельзя.</p>\n<table>\n<thead>\n<tr>\n<th>Поле</th>\n<th>Как думать</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>requests.cpu</code></td>\n<td>“мне нужно минимум столько CPU для планирования”</td>\n</tr>\n<tr>\n<td><code>requests.memory</code></td>\n<td>“мне нужно минимум столько памяти на node”</td>\n</tr>\n<tr>\n<td><code>limits.cpu</code></td>\n<td>“не давай контейнеру использовать CPU выше этого уровня”</td>\n</tr>\n<tr>\n<td><code>limits.memory</code></td>\n<td>“если контейнер превысит память, его могут убить”</td>\n</tr>\n</tbody>\n</table>\n<p>CPU нередко записывают в millicores:</p>\n<pre><code class=\"language-text\">100m = 0.1 CPU\n500m = 0.5 CPU\n1000m = 1 CPU\n</code></pre>\n<p>Память нередко записывают в <code>Mi</code> и <code>Gi</code>:</p>\n<pre><code class=\"language-text\">128Mi\n512Mi\n1Gi\n</code></pre>\n<h2>Как scheduler использует requests</h2>\n<p>Когда ты создаёшь Pod, scheduler подбирает node. Ориентируется он не на будущую реальную нагрузку приложения, а на <code>requests</code>.</p>\n<p>Если Pod просит:</p>\n<pre><code class=\"language-yaml\">requests:\n  memory: \"512Mi\"\n</code></pre>\n<p>то Kubernetes подыскивает node, где реально зарезервировать 512Mi памяти. Если подходящей node нет, Pod останется в состоянии <code>Pending</code>.</p>\n<p>Поэтому чрезмерно большие requests могут обернуться тем, что Pod вообще не запустится - даже если приложение “на самом деле почти ничего не ест”.</p>\n<h2>Что происходит при превышении limits</h2>\n<p>CPU и memory ведут себя по-разному.</p>\n<p>Когда контейнер выходит за <code>limits.cpu</code>, его обычно <strong>throttle'ят</strong>: ему достаётся меньше CPU-времени и он начинает работать медленнее.</p>\n<p>Когда контейнер выходит за <code>limits.memory</code>, его могут прибить с причиной <code>OOMKilled</code>. Затем Pod может перезапуститься, если это допускает политика перезапуска.</p>\n<p>Диагностика:</p>\n<pre><code class=\"language-bash\">kubectl describe pod web\nkubectl get pod web -o yaml\nkubectl logs pod/web --previous\n</code></pre>\n<p>В <code>describe</code> обращай внимание на <code>State</code>, <code>Last State</code>, <code>Reason</code>, <code>Restart Count</code> и <code>Events</code>.</p>\n<h2>Resources в Deployment</h2>\n<p>На практике resources чаще задают не в отдельном Pod, а в Deployment, внутри Pod template:</p>\n<pre><code class=\"language-yaml\">apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: web\nspec:\n  replicas: 2\n  selector:\n    matchLabels:\n      app: web\n  template:\n    metadata:\n      labels:\n        app: web\n    spec:\n      containers:\n        - name: nginx\n          image: nginx:alpine\n          resources:\n            requests:\n              cpu: \"100m\"\n              memory: \"128Mi\"\n            limits:\n              cpu: \"500m\"\n              memory: \"256Mi\"\n</code></pre>\n<p>Если поправить <code>resources</code> в <code>spec.template</code>, Deployment создаст новый ReplicaSet и проведёт rollout - точно как при смене image. Это существенно: ресурсы - часть шаблона Pod'а.</p>\n<p>Проверить resources можно так:</p>\n<pre><code class=\"language-bash\">kubectl get deployment web -o yaml\nkubectl describe pod -l app=web\nkubectl get pod -l app=web -o jsonpath='{.items[0].spec.containers[0].resources}'\n</code></pre>\n<h2>LimitRange — defaults и границы для контейнеров</h2>\n<p><code>LimitRange</code> действует на уровне namespace. Он способен задавать:</p>\n<ul>\n<li>default requests/limits, если контейнер не прописал их сам;</li>\n<li>минимальные и максимальные значения;</li>\n<li>допустимое соотношение request к limit.</li>\n</ul>\n<p>Пример:</p>\n<pre><code class=\"language-yaml\">apiVersion: v1\nkind: LimitRange\nmetadata:\n  name: default-limits\nspec:\n  limits:\n    - type: Container\n      defaultRequest:\n        cpu: \"50m\"\n        memory: \"64Mi\"\n      default:\n        cpu: \"200m\"\n        memory: \"256Mi\"\n      max:\n        cpu: \"1\"\n        memory: \"1Gi\"\n</code></pre>\n<p>Если Pod не указал <code>resources</code>, Kubernetes может подставить значения из LimitRange. Из-за этого новичок порой недоумевает: “я не писал limits, а они откуда-то взялись”. Это штатное поведение, когда в namespace настроен LimitRange.</p>\n<p>Настройкой LimitRange обычно занимается администратор платформы. Прикладному инженеру полезно уметь его просматривать:</p>\n<pre><code class=\"language-bash\">kubectl get limitrange\nkubectl describe limitrange\n</code></pre>\n<h2>ResourceQuota — бюджет namespace</h2>\n<p><code>ResourceQuota</code> ограничивает суммарный бюджет namespace:</p>\n<pre><code class=\"language-yaml\">apiVersion: v1\nkind: ResourceQuota\nmetadata:\n  name: team-quota\nspec:\n  hard:\n    pods: \"20\"\n    requests.cpu: \"4\"\n    requests.memory: \"4Gi\"\n    limits.cpu: \"8\"\n    limits.memory: \"8Gi\"\n</code></pre>\n<p>Смысл в том, что все Pod'ы namespace суммарно не могут запросить больше заданного бюджета.</p>\n<p>Как правило, ResourceQuota создаёт платформа или администратор кластера. Пользователю и прикладному инженеру чаще важно распознавать симптомы:</p>\n<pre><code class=\"language-bash\">kubectl get resourcequota\nkubectl describe resourcequota\nkubectl get events --sort-by=.lastTimestamp\n</code></pre>\n<p>Если создание Pod блокируется из-за quota, API Server обычно прямо указывает, какой именно лимит превышен.</p>\n<h2>Что важно для обычной работы</h2>\n<p>В повседневной работе ты чаще занимаешься не <code>ResourceQuota</code>, а вот этим:</p>\n<ol>\n<li>Открываешь Deployment manifest.</li>\n<li>Находишь контейнер в <code>spec.template.spec.containers</code>.</li>\n<li>Добавляешь или правишь <code>resources.requests</code> и <code>resources.limits</code>.</li>\n<li>Применяешь manifest.</li>\n<li>Проверяешь rollout и новые Pod'ы.</li>\n</ol>\n<p>Команды:</p>\n<pre><code class=\"language-bash\">kubectl apply -f deployment.yaml\nkubectl rollout status deployment/web\nkubectl describe pod -l app=web\n</code></pre>\n<p>Если Pod не поднимается, смотри:</p>\n<pre><code class=\"language-bash\">kubectl describe pod &lt;pod&gt;\nkubectl get events --sort-by=.lastTimestamp\n</code></pre>\n<h2>Типичные симптомы</h2>\n<table>\n<thead>\n<tr>\n<th>Симптом</th>\n<th>Что проверить</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Pod долго <code>Pending</code></td>\n<td>слишком большие <code>requests</code> или quota namespace</td>\n</tr>\n<tr>\n<td>Pod перезапускается с <code>OOMKilled</code></td>\n<td>memory limit меньше фактической потребности</td>\n</tr>\n<tr>\n<td>CPU хватает “рывками”</td>\n<td>возможно, контейнер упирается в CPU limit</td>\n</tr>\n<tr>\n<td>resources появились сами</td>\n<td>в namespace может быть LimitRange</td>\n</tr>\n<tr>\n<td>создание объекта запрещено</td>\n<td>RBAC или ResourceQuota</td>\n</tr>\n</tbody>\n</table>\n<h2>Связь уровней</h2>\n<pre><code class=\"language-text\">container resources\n  -&gt; requests помогают scheduler выбрать node\n  -&gt; limits ограничивают контейнер\nnamespace\n  -&gt; LimitRange задаёт defaults и границы\n  -&gt; ResourceQuota ограничивает общий бюджет\n</code></pre>\n<p>Дальше в лабораторной мы будем отрабатывать именно то, что пользователь реально может делать в собственном namespace: задавать requests/limits в Pod и Deployment, применять манифесты и проверять, какие ресурсы достались запущенному контейнеру.</p>f:[\"$\",\"div\",null,{\"className\":\"w-full min-w-0 flex-grow\",\"children\":[\"$\",\"div\",null,{\"className\":\"h-full w-full min-w-0 mx-auto\",\"children\":[\"$\",\"div\",null,{\"className\":\"h-full\",\"children\":[[\"$\",\"div\",null,{\"className\":\"max-w-7xl mx-auto w-full px-6\",\"children\":[[\"$\",\"div\",null,{\"className\":\"flex flex-row flex-wrap gap-2 items-center py-4\",\"children\":[[\"$\",\"$L27\",null,{\"courseId\":\"module_k8s_intro\",\"previousChapterId\":\"ch_k8si_quiz1\",\"nextChapterId\":\"ch_k8si_lab6\"}],[\"$\",\"$L28\",null,{\"courseId\":\"module_k8s_intro\",\"chapterId\":\"ch_k8si_4_ns\",\"isCompleted\":\"$undefined\",\"nextChapterId\":\"ch_k8si_lab6\"}]]}],[\"$\",\"h2\",null,{\"className\":\"text-2xl font-semibold mb-4 text-center\",\"children\":\"Namespaces и ограничение ресурсов\"}]]}],[\"$\",\"div\",null,{\"className\":\"h-full w-full max-w-7xl mx-auto px-3 sm:px-6 lg:px-0\",\"children\":[\"$\",\"$L29\",null,{\"content\":\"$2a\",\"courseId\":\"module_k8s_intro\",\"chapterId\":\"ch_k8si_4_ns\",\"isCompleted\":false,\"previousChapterId\":\"ch_k8si_quiz1\",\"nextChapterId\":\"ch_k8si_lab6\"}]}]]}]}]}]",
  "tasks": [],
  "vm_image": null,
  "vm_init": null
}