{
  "n": 12,
  "id": "ch_dock_4_layers",
  "title": "Слои, build cache и порядок инструкций",
  "type": "chapter",
  "isFree": false,
  "kind": "lesson",
  "content_html": "<h1>Docker: слои и кэширование</h1>\n\n\n<p>Написав первый Dockerfile, легко решить, что <code>docker build</code> лишь прогоняет инструкции по порядку сверху вниз и упаковывает всё в один крупный архив. В действительности Docker строит image из слоёв. Отсюда и следует, что повторная сборка нередко идёт быстрее исходной, что расположение <code>COPY</code> и <code>RUN</code> сказывается на скорости, а незначительная правка в начале Dockerfile способна заставить пересобрать почти всё.</p>\n<p>Разбираем это не ради “устройства Docker самого по себе”. Тема закрывает вполне прикладные вопросы: отчего образ раздулся до огромного размера, почему build cache не помог, из-за чего CI собирает так долго и каким образом составлять Dockerfile, чтобы сборки оставались воспроизводимыми и быстрыми.</p>\n<h2>Принцип работы слоёв</h2>\n<p>Любая инструкция <code>RUN</code>, <code>COPY</code>, <code>ADD</code> в Dockerfile порождает очередной <strong>слой (layer)</strong>. Слой представляет собой набор правок файловой системы: файлы, которые добавили, поменяли или удалили.</p>\n<pre><code class=\"language-bash\"># Посмотреть слои образа\ndocker history nginx:alpine\n\n# IMAGE         CREATED       CREATED BY                        SIZE\n# abc123       2 weeks ago   CMD [\"nginx\" \"-g\" \"daemon off;\"]  0B\n# def456       2 weeks ago   COPY file:xxx /etc/nginx/...      4.6kB\n# ghi789       2 weeks ago   RUN /bin/sh -c apk add --...      16.8MB\n# jkl012       2 weeks ago   /bin/sh -c #(nop) ENV NGINX...    0B\n# ...\n</code></pre>\n<p>Слои Docker складывает в <code>/var/lib/docker/overlay2/</code>. Идентификатором каждого слоя служит SHA256-хеш его содержимого. Именно это свойство и является ключевым: <strong>одинаковый контент = одинаковый хеш = один слой на диске</strong>.</p>\n<h3>Повторное использование слоёв</h3>\n<p>Когда два образа опираются на одни и те же слои, Docker держит их на диске в единственном экземпляре:</p>\n<pre><code>Образ A:                  Образ B:\nFROM ubuntu:22.04 ────────FROM ubuntu:22.04   ← ОДИН слой на диске\nRUN apt install curl      RUN apt install wget  ← разные слои\nCOPY app_a.py .           COPY app_b.py .       ← разные слои\n</code></pre>\n<p>Скачивая образ, Docker тянет лишь те слои, которых пока нет на машине. Если базовый <code>ubuntu:22.04</code> уже присутствует — повторно он не загружается.</p>\n<hr />\n<h2>Кэширование во время сборки</h2>\n<p>При каждом запуске <code>docker build</code> Docker сверяется с кэшем по каждой инструкции. Если слой с идентичным содержимым уже создавался ранее — Docker берёт его из кэша, а не выполняет команду заново.</p>\n<h3>Когда кэш сбрасывается</h3>\n<p>Кэш инвалидируется (перестаёт использоваться), если:</p>\n<ol>\n<li><strong>Поменялась сама инструкция</strong> — отредактирован текст <code>RUN</code>, <code>ENV</code>, <code>ARG</code></li>\n<li><strong>Поменялись файлы</strong> для <code>COPY</code>/<code>ADD</code> — Docker сопоставляет контрольные суммы</li>\n<li><strong>Сброшен предыдущий слой</strong> — стоит пересобрать один слой, и все идущие за ним собираются заново</li>\n</ol>\n<p>Третий пункт здесь главный. Слои образуют цепочку: как только меняется слой N, слои N+1, N+2, ... пересобираются целиком.</p>\n<pre><code>FROM python:3.11-slim          ← кэш: ✓\nWORKDIR /app                   ← кэш: ✓\nCOPY . .                       ← изменился app.py → CACHE MISS\nRUN pip install -r req.txt     ← пересобирается (даже если req.txt не менялся!)\nCMD [\"python\", \"app.py\"]       ← пересобирается\n</code></pre>\n<hr />\n<h2>Как упорядочить инструкции</h2>\n<p>Основной принцип: <strong>редко изменяемые инструкции ставим в начало, а часто изменяемые — ближе к концу.</strong></p>\n<h3>Неудачный вариант (сборка тормозит)</h3>\n<pre><code class=\"language-dockerfile\">FROM python:3.11-slim\nWORKDIR /app\n\n# Копируем ВСЁ сразу — любое изменение кода инвалидирует кэш pip install\nCOPY . .\nRUN pip install -r requirements.txt  # ← всегда пересобирается!\n\nCMD [\"python\", \"app.py\"]\n</code></pre>\n<p>Стоит поправить <code>app.py</code>, и Docker заново выполняет <code>pip install</code> — а это уходит в минуты.</p>\n<h3>Удачный вариант (сборка быстрая)</h3>\n<pre><code class=\"language-dockerfile\">FROM python:3.11-slim\nWORKDIR /app\n\n# Сначала только зависимости — они меняются редко\nCOPY requirements.txt .\nRUN pip install -r requirements.txt  # ← кэшируется, пока не изменился requirements.txt\n\n# Потом код — меняется часто\nCOPY . .\n\nCMD [\"python\", \"app.py\"]\n</code></pre>\n<p>Теперь правка <code>app.py</code> не сбрасывает кэш <code>pip install</code>. Пересобирается лишь <code>COPY . .</code> и то, что идёт следом.</p>\n<h3>Вариант для Node.js</h3>\n<pre><code class=\"language-dockerfile\">FROM node:20-alpine\nWORKDIR /app\n\n# Сначала package.json для npm install\nCOPY package.json package-lock.json ./\nRUN npm ci --only=production  # ← кэшируется\n\n# Потом исходный код\nCOPY src/ ./src/\n\nCMD [\"node\", \"src/index.js\"]\n</code></pre>\n<hr />\n<h2>Multi-stage builds</h2>\n<p><strong>Multi-stage build</strong> — это Dockerfile, где присутствует несколько блоков <code>FROM</code>. Каждый такой блок — самостоятельный «этап» сборки. В финальный образ попадает лишь содержимое последнего этапа (либо того, что указан явно).</p>\n<h3>В чём беда без multi-stage</h3>\n<p>Возьмём приложение на Go. Чтобы его скомпилировать, требуется компилятор Go (~300 МБ). Однако в production достаточно готового бинарника (~10 МБ):</p>\n<pre><code class=\"language-dockerfile\"># Без multi-stage — образ ~350 МБ\nFROM golang:1.21-alpine\nWORKDIR /app\nCOPY app.go .\nRUN go build -o myapp app.go\nCMD [\"./myapp\"]\n</code></pre>\n<p>В образе оказывается компилятор Go, который в production не пригодится.</p>\n<h3>Выход: multi-stage</h3>\n<pre><code class=\"language-dockerfile\"># Stage 1: сборка\nFROM golang:1.21-alpine AS builder\nWORKDIR /build\nCOPY app.go .\nRUN go build -o myapp app.go\n\n# Stage 2: production\nFROM alpine:latest\nWORKDIR /app\nCOPY --from=builder /build/myapp .\nCMD [\"./myapp\"]\n</code></pre>\n<p>Финальный образ строится на <code>alpine:latest</code> (~5 МБ) плюс бинарник (~10 МБ) = ~15 МБ вместо ~350 МБ.</p>\n<p>В обычном образе нередко лежат компилятор, package manager cache, исходники и временные файлы. Multi-stage даёт возможность сохранить лишь runtime-часть. За счёт этого размер image снижается, а лишних инструментов внутри контейнера становится меньше.</p>\n<p>Сопоставить результат помогают привычные команды:</p>\n<pre><code class=\"language-bash\">docker images\ndocker history app:single\ndocker history app:multi\n</code></pre>\n<h3>Этапы с именами</h3>\n<pre><code class=\"language-dockerfile\">FROM node:20-alpine AS deps\nWORKDIR /app\nCOPY package.json .\nRUN npm ci\n\nFROM node:20-alpine AS builder\nWORKDIR /app\nCOPY --from=deps /app/node_modules ./node_modules\nCOPY . .\nRUN npm run build\n\nFROM nginx:alpine AS production\nCOPY --from=builder /app/dist /usr/share/nginx/html\n</code></pre>\n<h3>Копирование из определённого этапа</h3>\n<pre><code class=\"language-dockerfile\"># Скопировать из именованного этапа\nCOPY --from=builder /build/myapp .\n\n# Скопировать из образа (не из этапа)\nCOPY --from=nginx:alpine /etc/nginx/nginx.conf /etc/nginx/nginx.conf\n</code></pre>\n<hr />\n<h2>Флаг --no-cache</h2>\n<p>Порой кэш только мешает: например, когда надо убедиться, что Dockerfile собирается с чистого листа, или выяснить, не маскирует ли локальный cache проблему с зависимостями.</p>\n<pre><code class=\"language-bash\"># Пересобрать инструкции Dockerfile без build cache\ndocker build --no-cache -t myapp:fresh .\n</code></pre>\n<p><code>--no-cache</code> запрещает задействовать build cache для инструкций Dockerfile. Приём полезный, но сборка при этом идёт дольше.</p>\n<p>Учти: <code>--no-cache</code> не отменяет необходимость обновлять base image. Когда требуется забрать свежую версию базового образа из registry, добавляют <code>--pull</code>:</p>\n<pre><code class=\"language-bash\">docker build --pull -t myapp:latest .\n\n# Максимально чистая проверка\ndocker build --pull --no-cache -t myapp:fresh .\n</code></pre>\n<p>Применяйте <code>--no-cache</code> не как повседневный режим разработки, а лишь в отдельных случаях:</p>\n<ul>\n<li>убедиться, что Dockerfile воспроизводимо собирается без локального cache;</li>\n<li>снять подозрение в том, что build cache сломан или устарел;</li>\n<li>обнаружить зависимость от внешнего состояния, которую cache прятал.</li>\n</ul>\n<p>Когда <code>docker build --no-cache</code> падает, а обычный <code>docker build</code> отрабатывает нормально, значит сборка опиралась на устаревший cache. Это повод проверить версии пакетов, доступность репозиториев и фиксацию зависимостей.</p>\n<hr />\n<h2>Как разобраться с cache miss</h2>\n<p>Если сборка внезапно замедлилась, для начала определи, какая инструкция первой перестала брать данные из cache. В современном Docker работает BuildKit, и для диагностики удобно включить детальный вывод:</p>\n<pre><code class=\"language-bash\">docker build --progress=plain -t myapp .\n</code></pre>\n<p>Затем пройдись по частым причинам:</p>\n<ol>\n<li>Не выполняется ли <code>COPY . .</code> для всего проекта ещё до установки зависимостей.</li>\n<li>Не изменяются ли файлы, на которые опираются <code>RUN npm ci</code>, <code>pip install</code> или <code>go mod download</code>.</li>\n<li>Не попали ли лишние файлы в build context из-за того, что нет <code>.dockerignore</code>.</li>\n<li>Не расположена ли часто изменяемая инструкция слишком высоко в Dockerfile.</li>\n</ol>\n<p>Для разных package managers логика одинакова:</p>\n<pre><code class=\"language-dockerfile\"># Python\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\nCOPY . .\n\n# Node.js\nCOPY package*.json ./\nRUN npm ci\nCOPY . .\n\n# Go\nCOPY go.mod go.sum ./\nRUN go mod download\nCOPY . .\nRUN go build -o app .\n</code></pre>\n<p>Вначале копируем файлы с описанием зависимостей. Далее ставим сами зависимости. И только затем копируем часто изменяемый код.</p>\n<p>Не смешивай <code>pip install --no-cache-dir</code> и <code>docker build --no-cache</code>. Первый ключ выключает внутренний cache <code>pip</code>, чтобы лишние файлы не попадали в image. Второй отключает Docker build cache на всю сборку.</p>\n<hr />\n<h2>Размер образа: полезные приёмы</h2>\n<h3>Берите минимальные базовые образы</h3>\n<pre><code>ubuntu:22.04   ~ 77 МБ\ndebian:bookworm-slim ~ 74 МБ\nalpine:latest  ~ 7 МБ\npython:3.11    ~ 1 ГБ  (с dev инструментами)\npython:3.11-slim ~ 130 МБ\npython:3.11-alpine ~ 50 МБ\n</code></pre>\n<h3>Склеивайте команды RUN</h3>\n<pre><code class=\"language-dockerfile\"># Плохо: 3 слоя, кэш apt-get сохраняется в образе\nRUN apt-get update\nRUN apt-get install -y curl wget\nRUN rm -rf /var/lib/apt/lists/*\n\n# Хорошо: 1 слой, apt-кэш не попадает в образ\nRUN apt-get update \\\n    &amp;&amp; apt-get install -y --no-install-recommends curl wget \\\n    &amp;&amp; rm -rf /var/lib/apt/lists/*\n</code></pre>\n<h3>Убирайте временные файлы в том же RUN</h3>\n<pre><code class=\"language-dockerfile\"># Плохо: файл добавлен в слой 1, \"удалён\" в слое 2 — но занимает место в слое 1\nRUN curl -o /tmp/setup.sh https://example.com/setup.sh\nRUN bash /tmp/setup.sh\nRUN rm /tmp/setup.sh\n\n# Хорошо: всё в одном слое\nRUN curl -o /tmp/setup.sh https://example.com/setup.sh \\\n    &amp;&amp; bash /tmp/setup.sh \\\n    &amp;&amp; rm /tmp/setup.sh\n</code></pre>\n<h3>.dockerignore</h3>\n<p>Всегда заводите <code>.dockerignore</code>. Раздутый контекст сборки тормозит <code>docker build</code>:</p>\n<pre><code>.git\nnode_modules\n*.log\n.env\ndist\nbuild\ncoverage\n.DS_Store\n</code></pre>\n<p><code>.dockerignore</code> важен не только ради скорости. Если секрет по недосмотру оказался в build context и был скопирован через <code>COPY</code>, он способен сохраниться в истории image даже после того, как его удалят в следующем слое.</p>\n<p>Неудачный пример:</p>\n<pre><code class=\"language-dockerfile\">COPY secrets.env /app/secrets.env\nRUN rm /app/secrets.env\n</code></pre>\n<p>Файл уже зафиксирован в предыдущем слое. Разумнее вовсе не передавать его в build context и не копировать в image.</p>\n<hr />\n<h2>Команды для разбора образов</h2>\n<pre><code class=\"language-bash\"># Размер образов\ndocker images --format \"table {{.Repository}}\\t{{.Tag}}\\t{{.Size}}\"\n\n# История слоёв (размер каждого)\ndocker history myapp:v1\n\n# Детальная информация\ndocker image inspect myapp:v1\n\n# Удалить неиспользуемые образы\ndocker image prune\n\n# Удалить всё неиспользуемое (контейнеры, образы, сети, volumes)\ndocker system prune\n</code></pre>\n<hr />\n<h2>Подведём итог</h2>\n<p>Разобравшись со слоями и кэшем, вы получаете ключ к быстрым сборкам:</p>\n<ol>\n<li><strong>Порядок инструкций имеет значение</strong> — редко изменяемые вперёд, часто изменяемые в конец</li>\n<li><strong>COPY зависимостей раньше COPY кода</strong> — тогда install попадёт в кэш</li>\n<li><strong>Multi-stage</strong> — отделяет окружение сборки от production и ужимает образ</li>\n<li><strong>Склеивайте RUN</strong> — меньше слоёв и меньше размер</li>\n<li><strong>.dockerignore</strong> — не тащите лишнее в контекст сборки</li>\n<li><strong><code>--no-cache</code> и <code>--pull</code></strong> — средства проверки, а не подмена грамотного порядка слоёв</li>\n</ol>f:[\"$\",\"div\",null,{\"className\":\"w-full min-w-0 flex-grow\",\"children\":[\"$\",\"div\",null,{\"className\":\"h-full w-full min-w-0 mx-auto\",\"children\":[\"$\",\"div\",null,{\"className\":\"h-full\",\"children\":[[\"$\",\"div\",null,{\"className\":\"max-w-7xl mx-auto w-full px-6\",\"children\":[[\"$\",\"div\",null,{\"className\":\"flex flex-row flex-wrap gap-2 items-center py-4\",\"children\":[[\"$\",\"$L24\",null,{\"courseId\":\"module_docker_basics\",\"previousChapterId\":\"ch_dock_lab3\",\"nextChapterId\":\"ch_dock_lab4\"}],[\"$\",\"$L25\",null,{\"courseId\":\"module_docker_basics\",\"chapterId\":\"ch_dock_4_layers\",\"isCompleted\":\"$undefined\",\"nextChapterId\":\"ch_dock_lab4\"}]]}],[\"$\",\"h2\",null,{\"className\":\"text-2xl font-semibold mb-4 text-center\",\"children\":\"Слои, build cache и порядок инструкций\"}]]}],[\"$\",\"div\",null,{\"className\":\"h-full w-full max-w-7xl mx-auto px-3 sm:px-6 lg:px-0\",\"children\":[\"$\",\"$L26\",null,{\"content\":\"$27\",\"courseId\":\"module_docker_basics\",\"chapterId\":\"ch_dock_4_layers\",\"isCompleted\":false,\"previousChapterId\":\"ch_dock_lab3\",\"nextChapterId\":\"ch_dock_lab4\"}]}]]}]}]}]\n28:I[65346,[\"423\",\"static/chunks/423-fc7ee96b2b57f1e6.js\",\"2619\",\"static/chunks/2619-04bc32f026a0d946.js\",\"3592\",\"static/chunks/app/(course)/courses/%5BcourseId%5D/layout-99d3e901d69f121d.js\"],\"SideNav\"]\n29:T1fb1f,data:image/webp;base64,",
  "tasks": [],
  "vm_image": null,
  "vm_init": null
}